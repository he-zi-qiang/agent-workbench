# Task settlement targets

A queued Task should be claimed within thirty seconds at the median, and a
settled Task should reach its terminal status within one poll interval of the
graph finishing.

Neither number bounds how long the work itself takes; they bound the waiting
around it.
