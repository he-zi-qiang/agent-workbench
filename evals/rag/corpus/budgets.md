# Ceilings

A run's budget is spent before the work, not accounted for after it. Tool call
quota is reserved before dispatch, so a turn proposing more calls than remain
has the excess refused rather than executed. Token ceilings are reconciled
after a model call, because what a call will cost is unknowable before making
it.
