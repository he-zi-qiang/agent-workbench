# Artifact Store

The Artifact Store holds uploaded bytes and produced artifacts behind content
digests. It is owned by team Marlin, and it returns the `AWB-44xx` family when a
transfer does not match what the client declared.

Its bytes live in the `aw-blobs` bucket.
