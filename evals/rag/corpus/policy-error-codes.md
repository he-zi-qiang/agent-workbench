# How refusal codes are numbered

Codes are `AWB-` followed by four digits. The first two digits identify the
family, and every family belongs to exactly one component -- which component is
recorded on that component's own page, not here.

A code is never reassigned to a different family.
